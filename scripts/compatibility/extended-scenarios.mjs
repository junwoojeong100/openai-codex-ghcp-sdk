// Contracts for native surfaces absent from the original core-10 suite.
export function extendedScenarios(scenario, text) {
  const s = (id, ko, en, seconds, turns, tools, fixture, prompt, steps, assertions, coverage) =>
    scenario(id, text(ko, en), seconds, turns, tools, text(fixture, fixture), prompt,
      steps.map(x => text(x, x)), assertions.map(([description, artifact]) => [text(description, description), artifact]),
      coverage, text("네이티브 동작을 우회하거나 대역/부분 성공을 실모델 통과로 오인", "Bypassed native behavior or false credit for doubles/partial success"), ["host", "exec"]);
  return [
    s("C11", "생산 launcher 기본 도구·프로세스 수명", "Production launcher defaults and process lifecycle", 120, 1, 6,
      "Isolated HOME/CODEX_HOME, the actual bin/codex-ghcp entry point, and an immutable secret.txt. Observation hooks do not replace live SDK behavior or tool metadata.",
      "Read secret.txt using the default native shell tool. Return its exact contents. Do not edit files.",
      ["Run the actual launcher and its child production bridge. Do not supply model_catalog_json or an apply_patch/shell override. Capture child SDK/HTTP records and CLI JSONL, and verify owned bridge exit."],
      [["The actual launcher exits zero, reads the hidden nonce and uses the selected SDK model with production tool defaults.", "launcher.json"],
       ["No catalog/tool-profile override was supplied; the owned bridge stopped and its endpoint is no longer listening.", "resources.json"]], ["launcher-defaults"]),
    s("C12", "네이티브 코드 리뷰", "Native reviewer entry point", 120, 1, 12,
      "One uncommitted bounds defect at review.mjs line 3; a separate correct file must not receive a finding.",
      "Native review/start against uncommittedChanges, inline delivery; no natural-language substitute for the reviewer RPC.",
      ["Observe review/start, enteredReviewMode and exitedReviewMode for the same native review turn. An unsupported structured-output request stays unsupported, never a successful review."],
      [["The native review RPC and mode lifecycle completed on the requested thread.", "review.json"],
       ["One actionable finding identifies review.mjs line 3 and its bounds bug, with a real diff read and no filesystem mutation.", "review.json"]], ["native-review"]),
    s("C13", "Plan 모드·사용자 확인 왕복", "Plan mode and user clarification round trip", 120, 1, 8,
      "Read-only fixture; the user's release code is hidden from the model until the host answers a native request_user_input callback.",
      "Plan a safe rollout; do not implement or edit anything. Use the native request_user_input tool exactly once, asking a question with id release_code for the user's release code. Wait for the answer, then include that exact code in your proposed plan. Do not guess the code.",
      ["Select native collaborationMode=plan using built-in mode instructions. Correlate the question, the host's bounded answer and its transport tool result; preserve every file."],
      [["The real Plan turn asks one correlated native question and receives the fixture user's answer.", "clarification.json"],
       ["The final plan contains that exact hidden answer; there are no edits or mutating commands.", "clarification.json"]], ["plan", "clarification"]),
    s("C14", "장문 문맥 압축 후 새 프로세스 재개", "Longer context, native compaction and fresh-process resume", 180, 3, 8,
      "A hidden memory nonce, at least 12 KiB of deterministic filler, and a persisted native thread. Delete the source before compaction and recreate both Codex and SDK afterward.",
      "Read memory.txt and remember its exact contents. After the filler, native compaction and process restart, return the remembered value without tools.",
      ["Generate real history, request thread/compact/start, wait for the correlated contextCompaction lifecycle and completion, then stop/recreate Codex and SDK and resume the same thread."],
      [["Native contextCompaction starts and completes after the recorded long input, without calling remote Responses compaction.", "compaction.json"],
       ["A fresh native/SDK process pair recalls the exact nonce from persisted compacted history without rereading or replaying tools.", "resume.json"]], ["compaction"]),
    s("C15", "실행 중 중단·소유 작업 정리·후속 턴", "Interrupt an active turn, clean owned work and continue", 120, 2, 10,
      "Owned long-task.mjs writes a start receipt and waits. The controller interrupts only after actual native command start, then explicitly cleans native background terminals.",
      "Run node long-task.mjs with the native command tool and yield_time_ms=10000. Wait for it; do not start other commands or modify the script.",
      ["Observe the long command and its on-disk receipt before turn/interrupt. Require interrupted completion, clean background terminals, prove the owned process exited, then run a successful follow-up on the same thread."],
      [["A real in-flight turn is interrupted by its exact thread/turn IDs, not by a whole-worker timeout.", "interruption.json"],
       ["The owned long process is gone and the same thread completes a fresh file-reading turn without duplicate side effects.", "interruption.json"]], ["interruption"]),
    s("C16", "HTTP MCP·Bearer 인증·오류 복구", "HTTP MCP, bearer authentication and error recovery", 120, 1, 10,
      "Owned loopback Streamable HTTP MCP endpoint. The temporary bearer secret is not passed to shell tools or persisted in evidence. An unauthenticated probe must fail.",
      "Use MCP fixture: list its resources, read fixture://config, call lookup with key missing, observe ENOENT, then call lookup with key selected. Return the resource code and lookup value. Do not use shell or other tools.",
      ["Use Codex's URL/bearer-token MCP configuration, not direct harness calls for the credited operations. Record authenticated discovery/resource/tool requests and native MCP items."],
      [["Native HTTP MCP discovery, resource reading and error-to-success tool calls are correlated with real server receipts.", "mcp.json"],
       ["Missing bearer authentication is rejected; all credited MCP operations are authenticated, with no credential disclosure or shell bypass.", "mcp.json"]], ["http-mcp"]),
    s("C17", "네이티브 서브에이전트 위임·결과 회수", "Native subagent delegation and result collection", 180, 1, 14,
      "A read-only child.txt nonce. Root must delegate its read, wait for the native child and close it; the root must not read the file itself.",
      "Use spawn_agent to delegate this read-only task: Read child.txt with a native shell tool and return its exact contents. Wait for that agent, close it, and report its result. Do not read child.txt yourself or edit any files. The parent may use only native subagent lifecycle tools, not shell, file or MCP tools.",
      ["Enable only the native multi_agent feature for this case. Require native spawn/wait/close events, a distinct child thread and independent exact-model SDK sessions."],
      [["Native agent lifecycle events correlate a distinct child thread, successful wait and close; no fabricated delegation text.", "agents.json"],
       ["The child really reads the hidden nonce, the root reports it, and every owned thread/SDK session is cleaned without mutations.", "agents.json"]], ["subagents"]),
    s("C18", "일시적 HTTP 오류 재시도·중복 실행 방지", "Transient HTTP retry without duplicate execution", 90, 1, 6,
      "The owned bridge returns exactly one marked 503 before SDK submission. The native provider has a bounded request retry; whole-case reruns remain disabled.",
      "Read secret.txt once with the native shell tool and reply with its exact contents. Do not retry the tool yourself.",
      ["Inject a single pre-inference transport failure. Compare failed/retried request bodies, observe native success, and count actual SDK sends and file reads rather than trusting the assistant's claim."],
      [["Exactly one native request retries the recorded 503 with an identical payload, then all remaining responses complete.", "retry.json"],
       ["One SDK prompt submission and one real read occur; the final nonce is correct and no tool side effect is duplicated.", "retry.json"]], ["retry"]),
  ];
}
