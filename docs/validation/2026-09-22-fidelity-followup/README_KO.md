# 추가 실제 검증 및 실패 경계 추적 — 2026-09-22

[English](README.md) · [검증 목록](../README_KO.md) · [안정성 계약](../../STABILITY_TESTING_KO.md)

## 결과: 95% 미달, 검증된 생산 코드 수정 없음

이 랩탑의 실제 Codex 환경에서 새로 실행한 `application-data-v1` 전체 결과는 **50/77 (64.94%), Opus 9/11**입니다. 목표인 최소 **74/77**에 도달하지 못했습니다. 77건을 모두 실행했으며 27건 실패, 차단·미지원·시간 초과·미실행은 각각 0건입니다.

| 실행 | 전체 | Opus |
|---|---:|---:|
| 이번 새 `application-data-v1` | **50/77 (64.94%)** | **9/11** |
| 이전 종료 기록의 `application-data-v1` — 별도 보존 | 50/77 (64.94%) | 9/11 |
| 원래/기본 v3 — 별도 보존, 이번에 재실행하지 않음 | 66/77 (85.71%) | 0/11 |

총점이 같아도 통과·실패한 셀은 다릅니다. 이번 결과는 이전 실행이나 진단의 통과 셀을 합친 점수가 아니며, 실패를 재채점하지 않았습니다. 기본 프로필은 여전히 `v3`입니다.

| 모델 | 이번 통과 |
|---|---:|
| gpt-5.6-sol | 10/11 |
| gpt-5.6-terra | 4/11 |
| gpt-5.6-luna | 5/11 |
| gpt-6-astra | 11/11 |
| claude-opus-5 | 9/11 |
| claude-sonnet-5 | 11/11 |
| claude-haiku-4.5 | 0/11 |

## 확인한 실패 경계

**출력 보존 실패 24건:** 22건은 `value:`·`receipt:` 레이블 누락, 2건은 receipt 줄 전체 누락입니다. 이 중 2건은 정리 검사에도 실패했습니다. 실패한 데이터 턴의 네이티브 최종 텍스트는 모두 실제 SDK assistant 메시지와 일치했습니다. 원문 대신 값을 출력한 것은 응답 변환기가 레이블을 제거한 결과가 아닙니다. Terra S07의 `before-loss`와 Luna S03에서는 SDK 출력부터 두 번째 줄이 빠졌습니다.

별도 Haiku 전송 관찰에서는 **73바이트 도구 결과가 실제 모델 요청까지 완전히 일치**했고, 공통 원문 보존 안내도 공급자 시스템 문맥에 포함됐습니다. 그런데 모델 응답에서 레이블이 빠졌으며 필터 신호는 없었습니다. 이 표본은 [전송 진단](diagnostics.json)에 해시·일치 여부만 공개합니다. 다른 모든 요청의 전송 내용까지 직접 관찰했다는 뜻은 아닙니다.

**Opus 필터 2건:** S10의 새 프로세스 재개 후 `recall`, S11의 같은 SDK 세션 `repeat-2`에서 명시적인 필터가 다시 발생했습니다. S11은 과거 이력 replay 없이도 실패했습니다. 관측된 원문·이력·phase 불일치나 안전하게 고칠 세션 결함은 확인하지 못했습니다. 이번 전체 행렬은 native refusal category를 수집하지 않았으므로 과거의 `reasoning_extraction` 분류를 소급 적용하지 않습니다.

**정리 RPC 시간 초과 3건:** 종료 시 자원 및 supervisor 프로세스 그룹은 모두 77/77 정리됐지만, 아래 5초 RPC 제한 위반은 그대로 실패입니다. 정리 계약 자체는 **74/77**이며 전체 성공률과 다른 지표입니다.

| 케이스 | 제한 | 관측 지연 | 다른 실패 |
|---|---:|---|---|
| Sol S06 | 작업별 5,000ms | disconnect 6,604ms; delete 5,002ms에서 시간 초과 보고 | 없음 |
| Terra S06 | 작업별 5,000ms | disconnect 5,036ms; delete 5,017ms에서 시간 초과 보고 | final-values |
| Luna S09 | 작업별 5,000ms | disconnect 5,016ms에서 시간 초과 보고 | final-values |

Terra S06에서는 ACK gate가 disconnect 시작보다 22ms 먼저 해제됐습니다. Luna S09에서는 장애 세션 정리가 정상 종료됐고, 복구 턴 완료 및 pending=0·queue=0 확인 후 **정상 종료 중 두 번째 세션**의 disconnect가 지연됐습니다. 따라서 ACK gate나 미완료 도구를 세 실패의 공통 원인으로 단정하지 않습니다. SDK 정리 RPC 지연은 확인했지만 근본 원인과 안전한 브릿지 순서 수정은 미확정입니다. 제한 시간을 늘리거나 정리 오류를 숨기지 않았습니다.

## 실제 개선 시도와 원복

4개 에이전트를 출력 변환, 세션 생명주기, 설치된 SDK 계약, 회귀 검사에 나눠 조사했습니다. 전체 행렬과 별도로 **21건의 실제 네이티브 진단**을 실행했습니다: 원본 5건, 공통 지시문 후보 9건, 예시를 포함한 두 추가 후보 6건, 전송 관찰 1건입니다. 이는 모델 HTTP 호출 수가 아니며 77건 점수에 더하지 않습니다.

원문 재현을 요청받았을 때만 레이블·문자·공백을 보존하도록 하는 **일반적인 공통 지시문 3안**을 실제로 시험했습니다. 모델별·fixture별 특수 문구는 넣지 않았고 사용자 요청·도구 결과·모델 출력도 다시 쓰지 않았습니다. 세 안 모두 Haiku의 핵심 실패를 해결하지 못했습니다. 추가 두 안의 선언·소스 동결·정확히 6건 실행·무재시도 증거를 보존했습니다.

효과가 입증되지 않은 안내와 임시 테스트는 모두 원복했습니다. **최종 생산 코드·테스트·실행기·판정기·의존성은 이전 종료 기록과 바이트 단위로 같습니다.** 새 전체 실행도 복원한 소스로 수행했습니다. 이번 점수를 코드 개선의 성과로 보고하지 않습니다.

현재 증거에서 안전하게 채택할 추가 생산 수정은 확인하지 못했습니다. 원문을 모델이 생략하는 현상, 상위 필터, 원인 미확정 정리 RPC 지연이 남아 있어 95% 목표는 차단 상태입니다. 모델 제외·필터 해제·출력 보정·자동 추론 재시도·새 프로필 점수 대입으로 목표를 맞추지 않았습니다.

## 최종 로컬 검사

| 검사 | 결과 |
|---|---:|
| 기존 단위·회귀 | **266/266**, 실패·건너뜀·취소 0 |
| 실제 Codex + 모의 SDK 호환성 | **18/18** |
| 실제 Codex + 모의 SDK `application-data-v1` 안정성 | **11/11** |
| 시나리오 설계·생성 문서 검사 | 통과 |
| 오프라인 스트레스 | 통과 |

스트레스는 도구 결과 100회, 동일 결과 재시도 100회, 대기 취소 10회, generation 복구 10회이며 잔여 queue/state/listener는 0입니다. 최종 명령별 로그·종료 코드·소요 시간은 직접 파일로 보존했습니다. 이 검사들의 실제 모델 호출은 0회이며 실모델 점수에 합산하지 않습니다.

## 증거와 재현

[77건 요약](summary.json) · [실패 분석](failure-analysis.json) · [분리된 진단](diagnostics.json) · [로컬 검사](local-checks.json) · [현재/동결 소스 검증](verification.json) · [최종 감사](final-audit.json) · [소스 해시](source-manifest.json) · [원시 증거 해시](evidence-manifest.json)

원시 자료와 일회성 진단 실행기는 ignored `.runtime/validation-improve-20260922/`에 보존했습니다. 게시 자료에는 계정 인증정보·원시 대화·공급자 시스템 프롬프트가 없습니다. 소스 및 사용자 설정 불변 확인, 현재/동결 검증기 일치, 이전 50/77 증거 무결성 재확인을 마쳤습니다. 해시는 제3자 인증이 아니며 이 결과는 장시간 soak나 제품 전체 신뢰성 인증이 아닙니다.

```sh
# 저장한 이번 실행을 원래 동결 소스로 검증. 유효한 미통과 결과의 종료 코드는 1.
node .runtime/validation-improve-20260922/live-confirmation/source-snapshot/scripts/stability.mjs \
  --verify .runtime/validation-improve-20260922/live-confirmation/report.json

# 새 실제 검증에는 새 폴더가 필요하며 Copilot 사용량이 발생합니다.
npm run test:stability -- --execute --profile application-data-v1 --output .runtime/application-followup-new
```

실행 ID: `c8f92af1-b96b-4e3b-8e5f-fd56270f40b7`

실행: 한국 시간 **2026-09-22 09:30:45–09:42:29**, 약 11분 45초.

구현 해시: `74a337f0e2c388707393ba51da9b1c539d4bb37b0979395651e6e1760eff549f`

프로필 계약 해시: `ae362ca209a9de118d4d6b74b6931c348ac0231e642023b1595e44766e37fc9a`

이전 기록: [Opus 종료 기록](../2026-09-22-opus-closeout/README_KO.md).
