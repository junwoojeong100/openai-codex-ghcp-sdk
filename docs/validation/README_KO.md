# 최종 검증 결과 — 2026-09-25

[English](README.md) · [단일 검증 안내](../VERIFICATION_KO.md) · [JSON](report.json)

## 기록된 결과

**codex-ghcp-essential-v1 · 36/36 · 통과** — **2026-09-25 08:19–08:25 KST**.

실제 Codex CLI/TUI → 운영 bridge → 실제 Copilot SDK → 정확한 Copilot 모델 경로로 전체 36건을 통과했습니다. 시나리오 ID 6개·정확한 모델·제한 시간·36/36 기준을 유지했으며 자동 케이스 재시도·대체 모델·점수 합산은 없습니다. V02에는 아래의 근거 확인을 거치는 두 턴 절차를 적용했습니다.

**`fullMatrixPassed=true` · `evidenceIntegrity=true` · 실패·차단·시간 초과·미실행: 0건.**

| 모델 | 통과 | 실패 케이스 |
| --- | --- | --- |
| claude-opus-5.5 | 6/6 | — |
| claude-sonnet-5 | 6/6 | — |
| claude-haiku-4.5 | 6/6 | — |
| gpt-6-astra | 6/6 | — |
| gpt-6-sol | 6/6 | — |
| gpt-6-luna | 6/6 | — |

**모든 모델의 V02에서 실제 실패 재현 → 수정 → 통과 근거를 확인했습니다.** 읽기 전용 진단에서 테스트 3개 중 2개 실패·네이티브 종료 코드 1·작업 공간 스냅샷 불변을 기록한 뒤에만 수정 요청을 전달했습니다. Native `apply_patch`가 `discount.mjs`를 바꿨고 최종 스냅샷에 다른 파일 변경은 없었습니다. 같은 프로세스에서 정확히 같은 명령으로 테스트 3개 통과·종료 코드 0을 확인했습니다. 아래 미디어에 Sonnet의 진단과 수정 단계를 모두 포함했습니다.

모델 경로·HTTP/SSE·문맥/감시 설정·상위 오류 처리·MCP 격리·작업 공간 보존·정리·실행을 포함한 공통 검사 9개도 모두 **36/36** 통과했습니다. 정확히 일치하는 미지원 자동 제목 요청 66건은 별도로 집계하며 지원 요청의 성공으로 계산하지 않았습니다.

## V02 절차와 근거

- **읽기 전용 진단:** 파일 읽기와 정확한 실패 테스트 명령만 요청하고 수정을 금지합니다. 네이티브 기록과 작업 공간 스냅샷을 보존합니다.
- **근거 확인:** 가려지지 않은 비정상 종료 코드, 테스트 3개의 실패 요약, 파일 불변, 패치 시도 없음을 요구합니다. 근거가 없으면 실패하며 보정 지시·가짜 실패 재현·시나리오 재시도는 하지 않습니다.
- **수정:** 근거 확인 후에만 native `apply_patch`와 같은 테스트 명령을 요청합니다. 진단 기록이 최종 이력 앞부분에 그대로 남는지, 테스트 3개 통과·테스트/사용자 파일 보존·숨은 샘플 응답까지 재계산합니다.

모든 모델에 기존 케이스 제한 시간과 `workspace-write` 샌드박스 안에서 같은 두 턴을 적용합니다. 첫 턴의 읽기 전용 지시는 체크포인트에서 확인하며 샌드박스를 바꿔 강제하지는 않습니다. 운영 bridge의 명령·결과도 바꾸지 않습니다. **단계별 코딩 절차를 검증한 것이지 복합 요청 한 번의 지시 준수를 보장한 것은 아닙니다.** JSON에 모델별 진단·최종 종료 코드를 담았고, 전체 체크포인트는 `.runtime/verification-final/v02-workflow.json`과 케이스별 facts에 있습니다.

실모델 전송·세션 오류와 전송·목록 복구 횟수는 모두 0입니다. 복구 안전성은 제어된 회귀 검사로 확인했으며 이번 실행에서 실서비스 장애 복구를 수행하지는 않았습니다. 공급자·네트워크 장애 제거를 주장하지 않습니다.

## 녹화와 스크린샷

이번 실행에서는 **전체 TUI 녹화 42개, 단계별 스크린샷 150장, 종료 스크린샷 42장**을 로컬에 저장했습니다. 아래 공개 미디어는 모두 이번 실행에서 가져왔습니다.

**[약 102초 요약 영상 보기](media/verification-summary.mp4)** · [미디어 출처와 해시](media/manifest.json)

101.76초 요약본은 실제 속도의 최대 11초 구간 8개에 해당 시점의 실제 스크린샷을 각각 2초씩 붙였고, 정지 화면임을 명시했습니다. 제목 막대는 원본 화면 밖에 있으며 음성은 없습니다. 기록된 전체 결과인 **36/36 — PASS**를 표시합니다. 대표 장면이며 36건 전체 녹화나 원본 근거를 대신하지 않습니다. 첫 V02 장면의 테스트 실패는 필수인 실패 재현이지 검증 케이스 실패가 아닙니다.

| 영상 시작 | 스크린샷 | 기록된 결과 |
| --- | --- | --- |
| 00:00 | [V01 · Opus 유니코드 응답](media/01-v01-claude-opus-5.5.png) | 통과 |
| 00:13 | [V02 · Sonnet 읽기 전용 진단](media/02-v02-baseline-claude-sonnet-5.png) | 필수 테스트 실패 재현, 종료 코드 1 |
| 00:26 | [V02 · Sonnet 수정과 테스트](media/03-v02-repair-claude-sonnet-5.png) | 테스트 3개 통과, 종료 코드 0; 케이스 통과 |
| 00:39 | [V03 · Haiku MCP](media/04-v03-claude-haiku-4.5.png) | 통과 |
| 00:52 | [V04 · Astra 모델/추론 수준 전환](media/05-v04-gpt-6-astra.png) | 통과 |
| 01:05 | [V05 · Sol 중단/계속](media/06-v05-gpt-6-sol.png) | 통과 |
| 01:18 | [V06 · Sol 압축/회상](media/07-v06-compact-gpt-6-sol.png) | 통과 |
| 01:31 | [V06 · Luna 새 프로세스 재개/회상](media/08-v06-resume-gpt-6-luna.png) | 통과 |

전체 영상 디코딩·파일 해시·선별 프레임과 스크린샷 OCR을 확인했으며 수동 시각 검수는 수행하지 않았습니다. 격리된 합성 작업 공간을 촬영했으며 원본에는 로컬 임시 경로가 보일 수 있습니다. 미디어 manifest는 각 공개 파일을 원래 실행·체크포인트와 연결합니다.

## 로컬 회귀와 근거

단위·통합·문서 **376/376**, 실제 Codex와 SDK 대역을 사용하는 runtime **21/21** 통과. 실모델 검증 전에 같은 소스 지문으로 기존 시간 판정을 완화하지 않고 순차 실행했습니다. 로컬 결과를 실모델 점수에 더하지 않습니다.

Codex 0.154.0 · Copilot SDK 1.0.14 · darwin/arm64 · Node v22.16.0

기록된 실모델 결과는 이 로컬 환경에 한정됩니다. Linux·macOS CI 작업은 설정돼 있지만 여기서 원격 CI를 실행했다고 주장하지 않습니다.

증거는 자동 재계산했으며 `evidenceIntegrity=true`입니다. 원본과 동결 소스는 `.runtime/verification-final/`, 구현 지문은 `177abfa900a8a4e7de4868096ba459d9d9308f563b54ee44e40eda287e9a50dd`입니다. 지문은 Git 커밋 ID가 아닙니다. 실행 중 소스·사용자 설정은 바뀌지 않았고 소유한 작업 프로세스 그룹 36개는 모두 정리됐습니다. 공개 JSON은 원시 증거를 대체하지 않습니다.

## 증거 재검증

이 명령에는 원래 로컬 `.runtime/verification-final/` 폴더가 필요하며 Git에는 포함되지 않습니다. 모델 호출 없이 저장된 결과를 재계산합니다. 새 clone의 공개 요약·미디어만으로 무결성 검사를 재현할 수는 없습니다.

```bash
npm run verify -- --verify .runtime/verification-final/report.json
```

최종 문서 점검에서도 실행·검증·테스트 소스 지문과 원본 36/36 판정은 보존했습니다. 가이드 표현·공개 결과 메타데이터·로컬 배포 점검 기록을 갱신했으며 실모델 행렬이나 녹화를 다시 실행하지 않았습니다. 공개 미디어도 이 실행에서 발행한 파일과 바이트 단위로 동일합니다.

## 정리와 한계

현재 단일 검증·최신 근거·해당 미디어만 남겼습니다. 한 번의 핵심 연동 성공 결과이지 향후 모델 동작·요약 정확성·5시간 내구성·제품 전체 완성도를 보장하지는 않습니다. 기록된 검증에는 원격 CI 실행이나 기존 사용자 bridge 재시작이 포함되지 않습니다.
