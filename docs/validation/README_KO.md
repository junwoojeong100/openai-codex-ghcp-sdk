# 최종 검증 결과 — 2026-09-25

[English](README.md) · [단일 검증 안내](../VERIFICATION_KO.md) · [JSON](report.json)

## 기록된 결과

**codex-ghcp-essential-v1 · 34/36 · 미통과** — **2026-09-25 10:58–11:06 KST**.

실제 Codex CLI/TUI → 운영 bridge → 실제 Copilot SDK 경로와 정확한 모델 선택으로 전체 36건을 한 번씩 실행했습니다. 수정한 소스를 검증하되 시나리오 6개·제한 시간·36/36 통과 기준은 바꾸지 않았습니다. 자동 케이스 재시도·대체 모델·보정 지시·점수 합산 없이 두 실패를 최종 결과에 그대로 남겼습니다.

**`fullMatrixPassed=false` · `evidenceIntegrity=true` · 실패: 2건 · 차단·시간 초과·미실행: 0건.**

| 모델 | 통과 | 실패 케이스 |
| --- | --- | --- |
| claude-opus-5.5 | 5/6 | V04 |
| claude-sonnet-5 | 6/6 | — |
| claude-haiku-4.5 | 6/6 | — |
| gpt-6-astra | 6/6 | — |
| gpt-6-sol | 6/6 | — |
| gpt-6-luna | 5/6 | V02 |

## 실패 내역

| 케이스 | 기록된 결과와 해석 범위 |
| --- | --- |
| Opus V04 | 최초 SDK `listModels`가 10,570ms 후 `rpc_error`로 실패했습니다. 입력창·모델 호출·피커에 도달하기 전에 실행기가 종료됐습니다. V04는 Astra에서 시작해 Opus로 전환하므로 Opus 전환은 검증하지 못했습니다. 더 구체적인 네트워크·인증 원인은 이 진단만으로 확정할 수 없습니다. |
| Luna V02 | 필수 실패 재현, native `apply_patch` 수정, 테스트 3개 통과까지 성공했습니다. 그러나 네이티브 읽기 결과에 `sample.txt` 값이 있었는데도 최종 답변으로 그 값 대신 수정 전 `discount.mjs` 소스를 반환했습니다. `execution`·`V02.edit`가 실패했으며 답변 보정이나 재시도는 하지 않았습니다. |

JSON에 실패 검사 ID·시작 진단·Luna의 기대/실제 답변을 남겼습니다. 아래 실패 화면과 영상 구간은 재구성한 화면이 아니라 같은 실행의 원본입니다.

## 확인된 동작과 한계

모든 모델의 V02에서 읽기 전용 진단 시 파일 불변, 테스트 3개 중 2개 실패·종료 코드 1, 네이티브 패치 후 같은 테스트 3개 통과·종료 코드 0을 기록했습니다. `discount.mjs`만 변경됐고 테스트·사용자 파일은 보존됐습니다. 최종 답변도 필수이므로 **V02 전체 작업 흐름은 5/6 통과**입니다. 전체 체크포인트는 `.runtime/verification-final/v02-workflow.json`과 케이스별 facts에 있습니다. 두 턴 절차는 단계별 작업 흐름을 검증하며 일반적인 지시 준수 신뢰성을 보장하지 않습니다.

V01·V03·V05·V06은 모델 6개 모두 통과했습니다. 모델 경로·HTTP/SSE·문맥 tier·감시 설정은 35/36, 실행은 34/36 통과입니다. 작업 공간 보존·MCP 격리·정리는 36/36 통과했습니다. 정리나 일부 검사 성공이 케이스 실패를 덮어쓰지는 않습니다.

Sonnet V04와 Haiku V04의 시작 시 모델 목록 시간 초과 2건은 원래 30초 예산 안에서 복구됐습니다. 별도의 Opus V04 RPC 오류는 시간 초과 전용 복구 대상이 아니었습니다. 모델 전송·세션 오류와 모델 전송 복구는 없었지만, 목록 조회 단계의 시작 실패는 그대로 실패입니다. 정확히 일치하는 미지원 자동 제목 요청 64건은 별도로 집계하며 지원 응답 성공으로 계산하지 않습니다.

## 녹화와 스크린샷

이번 실행에서는 **전체 TUI 녹화 42개, 단계별 스크린샷 144장, 종료 스크린샷 42장**을 로컬에 저장했습니다. 공개 스크린샷 10장과 편집 영상은 모두 같은 실행에서 가져왔으며 두 실패도 포함합니다.

**[편집 요약 영상 보기](media/verification-summary.mp4)** · [미디어 출처·구간 시각·해시](media/manifest.json)

실제 속도의 각 영상 구간 뒤에 해당 체크포인트나 실패 화면을 2초간 붙이고 정지 화면임을 명시했습니다. 제목 막대는 전체 원본 화면 밖에 있으며 음성은 없습니다. 전체 판정은 **34/36 — NOT PASSED**이고 각 케이스 상태도 표시합니다. 대표 장면이며 36건 전체 녹화는 아닙니다. Sonnet의 진단 테스트 실패는 필수 근거이고, 마지막 두 장면은 실제 케이스 실패입니다.

| 장면 | 스크린샷 | 기록된 결과 |
| --- | --- | --- |
| 1 | [V01 · Opus 유니코드 응답](media/01-v01-claude-opus-5.5.png) | 통과 |
| 2 | [V02 · Sonnet 읽기 전용 진단](media/02-v02-baseline-claude-sonnet-5.png) | 필수 테스트 실패 재현, 종료 코드 1 |
| 3 | [V02 · Sonnet 수정과 테스트](media/03-v02-repair-claude-sonnet-5.png) | 테스트 3개 통과, 종료 코드 0; 케이스 통과 |
| 4 | [V03 · Haiku MCP](media/04-v03-claude-haiku-4.5.png) | 통과 |
| 5 | [V04 · Astra 모델/추론 수준 전환](media/05-v04-gpt-6-astra.png) | 통과 |
| 6 | [V05 · Sol 중단/계속](media/06-v05-gpt-6-sol.png) | 통과 |
| 7 | [V06 · Sol 압축/회상](media/07-v06-compact-gpt-6-sol.png) | 통과 |
| 8 | [V06 · Luna 새 프로세스 재개/회상](media/08-v06-resume-gpt-6-luna.png) | 통과 |
| 9 | [V04 · Opus 시작 실패](media/09-v04-failure-claude-opus-5.5.png) | 추론 전 실패: SDK 모델 목록 RPC |
| 10 | [V02 · Luna 최종 답변 불일치](media/10-v02-failure-gpt-6-luna.png) | 테스트 통과, 필수 답변 실패 |

모든 원본 녹화와 요약 영상 전체를 디코딩하고 원본 파일 해시와 스크린샷 10장·대표 영상 프레임 20개의 OCR을 확인했습니다. 직접 이미지 열람을 사용할 수 없어 수동 시각 검수를 했다고 주장하지 않습니다. 격리된 합성 작업 공간을 촬영했으며 로컬 임시 경로는 보입니다. 미디어 manifest는 공개 파일을 원래 실행과 체크포인트 또는 종료 시 실패 화면에 연결합니다.

## 로컬 회귀와 근거

단위·통합·문서 **392/392**, 실제 Codex와 SDK 대역을 사용하는 runtime **22/22** 통과. 상주 bridge 관리·정리 실패 표시 수정을 포함한 같은 소스 지문으로 실모델 검증 전에 순차 실행했습니다. 오프라인 성공을 실모델 점수에 더하거나 두 실패를 지우는 데 사용하지 않습니다.

Codex 0.154.0 · Copilot SDK 1.0.14 · darwin/arm64 · Node v22.16.0

증거를 자동 재계산한 결과는 `evidenceIntegrity=true`, `fullMatrixPassed=false`입니다. 원본과 동결 소스는 `.runtime/verification-final/`, 구현 지문은 `b4e39c1ca3cd6cedfd18e2d03871a7a14c19733f52b56db6a6209da9f542fa88`입니다. 지문은 Git 커밋 ID가 아닙니다. 실행 중 소스·사용자 설정은 바뀌지 않았고 소유한 작업 프로세스 그룹 36개는 모두 정리됐습니다. 공개 JSON은 원시 증거를 대체하지 않습니다.

이 기록에는 최신 로컬 검증만 보존합니다. 특정 커밋의 원격 CI 상태는 [GitHub Actions](https://github.com/junwoojeong100/openai-codex-ghcp-sdk/actions)에서 실모델 점수와 별도로 확인합니다.

## 증거 재검증

이 명령에는 원래 로컬 `.runtime/verification-final/` 폴더가 필요하며 Git에는 포함되지 않습니다. 모델 호출 없이 저장된 결과를 재계산합니다. 새 clone의 공개 요약·미디어만으로 무결성 검사를 재현할 수는 없습니다.

```bash
npm run verify -- --verify .runtime/verification-final/report.json
```

현재 소스와 동결 소스의 검증기가 같은 결과를 재현합니다. 이번 실행을 재검사하면 **종료 코드 1**을 반환합니다. 근거는 온전하지만 34/36은 통과가 아니기 때문입니다. 재검사는 모델을 호출하거나 두 실패를 바꾸지 않습니다.

## 정리와 한계

현재 단일 검증·이번 최종 실모델 실행·해당 미디어만 남겼습니다. Git 이력을 다시 쓰지 않고 이전 원본 결과·과거 공개 미디어·임시 게시 작업 파일을 제거했습니다. 기존 사용자 bridge는 재시작하지 않았습니다. 핵심 연동 평가가 미통과한 결과이며 제품 전체나 장기 내구성 인증이 아닙니다. 미지원 기능의 범위도 바뀌지 않았습니다.
