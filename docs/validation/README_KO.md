# 최종 검증 결과 — 2026-09-25

[English](README.md) · [단일 검증 안내](../VERIFICATION_KO.md) · [JSON](report.json)

## 기록된 결과

**codex-ghcp-essential-v1 · 36/36 · 통과** — **2026-09-25 12:30–12:40 KST**.

실제 Codex CLI/TUI → 운영 bridge → 실제 Copilot SDK → 정확한 모델 경로로 새 실행의 전체 36건을 통과했습니다. 시나리오 ID 6개·모델·제한 시간·36/36 기준은 유지했습니다. 아래의 프롬프트 명확화와 엄격해진 근거 검사는 명시적인 변경이며 숨긴 재채점이 아닙니다. 케이스 재시도·대체 모델·실패 후 보정 지시·점수 합산은 없습니다.

**`fullMatrixPassed=true` · `evidenceIntegrity=true` · 실패·차단·시간 초과·미실행: 0건.**

| 모델 | 통과 | 실패 케이스 |
| --- | --- | --- |
| claude-opus-5.5 | 6/6 | — |
| claude-sonnet-5 | 6/6 | — |
| claude-haiku-4.5 | 6/6 | — |
| gpt-6-astra | 6/6 | — |
| gpt-6-sol | 6/6 | — |
| gpt-6-luna | 6/6 | — |

## 이전 실패의 원인 검토

| 영역 | 진단과 개선 |
| --- | --- |
| Opus V04 시작 | 이전 실패는 추론·피커 조작 전 SDK `listModels`에서 발생했습니다. V04는 Astra에서 시작한 뒤 Opus로 전환합니다. 당시 진단에서 RPC 코드를 버렸고 동시 읽기 전용 조회 6회가 성공했으므로 네트워크·인증 등의 구체 원인은 복원할 수 없습니다. 이제 제한된 오류 메타데이터를 남기고, 식별된 읽기 전용 목록 오류는 정리 확인 후 원래 예산 안에서 새 client로 한 번 재시도합니다. 알려진 거절·취소는 제외하며 추론 재시도는 추가하지 않았습니다. |
| Luna V02 답변 | 이전 SDK 답변과 화면 모두 `sample.txt` 대신 소스 코드를 담았고, 코드 수정·테스트는 성공했습니다. 당시 실패 판정은 정상이었습니다. 이제 앞선 기억에 의존하지 않도록 수정 요청에 테스트 전 파일 재읽기를 명시하며, 파일 값은 화면의 완료 표지와 별도 식별자를 사용합니다. 정확한 출력은 여전히 필수이며 `V02.answer`로 분리해 판정합니다. |
| 실행기 답변 검사 | 앞선 일치 메시지나 다른 설명 사이의 정답 한 줄을 인정할 수 있었습니다. 최종 HTTP 텍스트 전체와 마지막 새 SDK 답변을 비교하고, 실패 답변도 facts에 보존하며, 화면 반영 지연과 실제 오답을 구분하도록 바꿨습니다. 회귀 검사는 오래된 답변·해설만의 일치를 거절합니다. |
| 네이티브 출력 근거 | 실제 Codex의 명령 완료 이벤트에는 이미 스트리밍한 앞부분이 빠질 수 있습니다. 고정 CLI로 재현했으며 새 runtime 검사의 오탐 원인이었습니다. 이제 파일 읽기는 호출 ID가 연결된 네이티브 도구 결과로, 명령·종료 코드는 명령 이벤트로 확인합니다. 빠진 바이트를 만들어 넣지 않습니다. |

복구되지 않은 목록 시작 오류는 상위 오류 검사도 실패시킵니다. 복구 기록의 프로세스·generation·순서가 일치해야 하며 새 프로세스 재개의 기록을 다른 프로세스의 복구 근거로 사용할 수 없습니다. 브라우저 렌더링 오류는 즉시 실패시키고 캡처 오류에는 체크포인트를 표시합니다. 실패를 면제한 것이 아니라 실행기를 강화했으며 이전 실패를 성공으로 바꾸지 않았습니다.

## 확인된 동작과 한계

모든 V02에서 읽기 전용 진단의 파일 불변, 테스트 3개 중 2개 실패·종료 코드 1, 네이티브 패치, 샘플 재읽기, 같은 테스트 3개 통과·종료 코드 0을 기록했습니다. `discount.mjs`만 변경됐고 테스트·사용자 파일은 보존됐습니다. **코딩 작업 흐름과 정확한 최종 답변 모두 6/6 통과**입니다. 전체 체크포인트는 `.runtime/verification-final/v02-workflow.json`과 케이스별 facts에 있습니다. 두 턴은 같은 `workspace-write` 샌드박스를 사용하며 진단 단계는 샌드박스 전환이 아닌 근거로 확인합니다.

시나리오 6개와 공통 검사 9개를 모두 통과했습니다. **도구 결과 제출 45/45건에서 네이티브 출력·HTTP 입력·SDK RPC의 바이트와 호출 식별자가 일치**하며 `.runtime/verification-final/tool-delivery.json`에 기록했습니다. 전달을 검증한 것이지 모델 내부의 이해나 모든 지시 준수를 보장한 것은 아닙니다.

이번 실행의 **bridge 목록 복구·모델 전송 복구는 모두 0건**입니다. 읽기 전용 복구의 장애 경로는 제어된 회귀 검사로 확인했으며 이전 실모델 장애를 재현했다고 주장하지 않습니다. 정확히 일치하는 미지원 자동 제목 요청 66건은 별도로 집계하며 지원 모델 응답의 성공으로 계산하지 않습니다.

## 녹화와 스크린샷

이번 실행에서는 **전체 TUI 녹화 42개, 단계별 스크린샷 150장, 종료 스크린샷 42장**을 로컬에 저장했습니다. 공개 스크린샷 8장과 편집 영상은 모두 이번 실행에서 가져왔습니다.

**[편집 요약 영상 보기](media/verification-summary.mp4)** · [미디어 출처·구간 시각·해시](media/manifest.json)

실제 속도의 각 영상 구간 뒤에 해당 체크포인트를 2초간 붙이고 정지 화면임을 명시했습니다. 제목 막대는 전체 원본 화면 밖에 있으며 음성은 없습니다. 전체 판정은 **36/36 — PASS**이고 각 케이스 상태도 표시합니다. 대표 장면이며 36건 전체 녹화는 아닙니다. Sonnet의 진단 테스트 실패는 필수 근거이지 케이스 실패가 아닙니다.

| 장면 | 스크린샷 | 기록된 결과 |
| --- | --- | --- |
| 1 | [V01 · Opus 유니코드 응답](media/01-v01-claude-opus-5.5.png) | 통과 |
| 2 | [V02 · Sonnet 읽기 전용 진단](media/02-v02-baseline-claude-sonnet-5.png) | 필수 테스트 실패 재현, 종료 코드 1 |
| 3 | [V02 · Sonnet 수정과 테스트](media/03-v02-repair-claude-sonnet-5.png) | 테스트 3개 통과, 종료 코드 0; 케이스 통과 |
| 4 | [V03 · Haiku MCP](media/04-v03-claude-haiku-4.5.png) | 통과 |
| 5 | [V04 · Astra 모델/추론 수준 전환](media/05-v04-gpt-6-astra.png) | 통과 |
| 6 | [V05 · Sol 중단/계속](media/06-v05-gpt-6-sol.png) | 통과 |
| 7 | [V06 · Sol 압축/회상](media/07-v06-compact-gpt-6-sol.png) | 통과 |
| 8 | [V06 · Luna 새 프로세스 재개/회상](media/08-v06-resume-gpt-6-luna.png) | 통과 |

모든 원본 녹화와 요약 영상 전체를 디코딩하고 원본 파일 해시와 스크린샷 8장·대표 영상 프레임 16개의 OCR을 확인했습니다. 수동 시각 검수를 했다고 주장하지 않습니다. 격리된 합성 작업 공간을 촬영했으며 로컬 임시 경로는 보입니다. 미디어 manifest는 공개 파일을 이번 실행의 실제 체크포인트와 연결합니다.

## 로컬 회귀와 근거

단위·통합·문서 **423/423**, 실제 Codex와 SDK 대역을 사용하는 runtime **22/22** 통과. 최종 검사들을 같은 소스 지문으로 실모델 행렬 전에 순차 실행했습니다. 오프라인 검사는 오답 거절·부분 네이티브 출력·결과 바이트 보존·목록 복구 제외 조건·캐시된 업데이트가 있는 시작/재개도 다룹니다. 오프라인 성공을 실모델 점수에 더하지 않습니다.

Codex 0.154.0 · Copilot SDK 1.0.14 · darwin/arm64 · Node v22.16.0

증거를 자동 재계산한 결과는 `evidenceIntegrity=true`, `fullMatrixPassed=true`입니다. 원본·진단·동결 소스는 `.runtime/verification-final/`, 구현 지문은 `8911f4768cae37f7184a9099b994d3deeca1ce15bde70c048106aa1d506397f7`입니다. 지문은 Git 커밋 ID가 아닙니다. 실행 중 소스·사용자 설정은 바뀌지 않았고 소유한 작업 프로세스 그룹 36개는 모두 정리됐습니다. 공개 JSON은 원시 증거를 대체하지 않습니다.

이 기록에는 최신 로컬 검증만 보존합니다. 특정 커밋의 원격 CI 상태는 [GitHub Actions](https://github.com/junwoojeong100/openai-codex-ghcp-sdk/actions)에서 실모델 점수와 별도로 확인합니다.

## 증거 재검증

이 명령에는 원래 로컬 `.runtime/verification-final/` 폴더가 필요하며 Git에는 포함되지 않습니다. 모델 호출 없이 저장된 결과를 재계산합니다. 새 clone의 공개 요약·미디어만으로 무결성 검사를 재현할 수는 없습니다.

```bash
npm run verify -- --verify .runtime/verification-final/report.json
```

현재 소스와 동결 소스의 검증기가 같은 통과 결과를 재현합니다. 보존한 실행의 재검사는 **종료 코드 0**을 반환하며 모델을 호출하거나 이전 실행을 재채점하지 않습니다.

## 정리와 한계

현재 단일 검증·최신 실모델 실행·해당 미디어만 보존하며, 진단은 별도의 점수를 가진 실행을 남기지 않고 검토한 실패 원리를 설명합니다. Git 이력을 다시 쓰지 않고 이전 원본 결과·과거 미디어·임시 게시 작업 파일을 제거했습니다. 기존 사용자 bridge는 재시작하지 않았습니다. 핵심 연동의 근거이지 제품 전체·모든 모델 답변의 정확성·장기 내구성 인증은 아니며 미지원 기능의 범위도 바뀌지 않았습니다.
