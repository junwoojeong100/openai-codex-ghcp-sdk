# 실제 검증 결과 — 2026-09-21

[English](README.md) · [70건 결과 JSON](summary.json) · [증거 hash 목록](evidence-manifest.json) · [시나리오](../../NATIVE_SCENARIOS_KO.md)

**실제 70건 완료: 57건 통과, 13건 실패. 소요 450.686초(약 7분 31초).** 차단·미지원·시간 초과·미실행은 모두 0건입니다.

전체 통과는 아닙니다. 모델별 10/10을 달성한 모델이 없으므로 `core-10-compatible` 판정은 성립하지 않습니다. 90%는 일상 개발 작업의 설계 목표이며 제품 커버리지 실측값이 아닙니다.

## 실행 정보

- Run ID: `4b7856b2-0183-49ac-acea-92ca26b130a2`
- KST: 2026-09-21 00:35:58.141 → 00:43:28.804
- Codex: `0.154.0`; Copilot SDK: `1.0.14`
- 단일 구성: 시나리오 10개 × GHCP 7모델. 별도 기준선·빠른 모드·부분 선택·케이스 재시도 없음.
- 최대 4모델 병렬. 전체 시간 강제 종료 없이 개별 타임아웃 적용.
- 저장된 증거를 독립 판정기로 재계산하여 무결성 확인. 실행 중 구현과 관찰한 사용자 설정은 불변, 사용자 중단 없음.

## 모델별 원래 판정

| 모델 | 통과 | 실패 | 실패 시나리오 |
|---|---:|---:|---|
| gpt-5.6-sol | 9/10 | 1 | C03 |
| gpt-5.6-terra | 9/10 | 1 | C03 |
| gpt-5.6-luna | 9/10 | 1 | C03 |
| gpt-6-astra | 9/10 | 1 | C03 |
| claude-opus-5 | 8/10 | 2 | C03, C05 |
| claude-sonnet-5 | 6/10 | 4 | C03, C04, C05, C10 |
| claude-haiku-4.5 | 7/10 | 3 | C03, C04, C10 |

## 시나리오별 결과

| 시나리오 | 작업 | 통과/7 |
|---|---|---:|
| C01 | CLI 시작·정확한 모델 선택·유니코드 스트리밍 | 7/7 |
| C02 | AGENTS 지시문·Skill 실행·비신뢰 입력 방어 | 7/7 |
| C03 | 저장소 탐색·코드 이해·Git diff 리뷰 | 0/7 |
| C04 | 다중 파일 리팩터링·생성·이동·삭제·Git 보호 | 5/7 |
| C05 | 실패 재현→디버깅→수정→회귀 테스트 | 5/7 |
| C06 | 함수 도구·MCP 리소스/도구·오류 복구 | 7/7 |
| C07 | 승인 거절·단일 동작 허용·우회 방지 | 7/7 |
| C08 | 파일·네트워크 샌드박스와 프로세스 정리 | 7/7 |
| C09 | 대화 기억·지시 변경·다른 세션 격리 | 7/7 |
| C10 | 재시작 후 resume·부작용 중복 방지 | 5/7 |

## 실패 해석과 시험의 한계

- **C03 (7모델):** 엄격한 JSON-only/필드 타입 조건 미충족이 주된 관찰입니다. `empty (empty.txt)`라는 프롬프트는 boolean 요구가 불명확합니다. Markdown fence를 사용한 올바른 내용도 실패합니다. `git -c ... diff` 같은 유효한 Git 명령이 좁은 명령 정규식에 걸리지 않는 한계도 확인했습니다. 이를 7모델의 코드 이해 불가나 bridge 결함으로 단정하면 안 됩니다.
- **C04 (Sonnet/Haiku):** 두 모델 모두 도구 이벤트 10개로 한도 9개를 초과했습니다. Sonnet은 파일 동작 조건 자체는 통과했습니다. Haiku는 대상 파일을 만들었지만 원래 `notes.txt`도 남겨 이동이 아닌 복사 상태여서 기능 조건도 실패했습니다.
- **C05 (Opus/Sonnet):** 실제 실패 로그와 수정 후 3개 테스트 통과·독립 입력 성공은 존재합니다. 다만 `node --test | tail` 파이프라인이 초기 실패 exit를 0으로 바꿔, 요구한 nonzero 종료 증거 조건에 실패했습니다. 버그 수정 자체가 실패했다는 뜻은 아닙니다.
- **C10 (Sonnet/Haiku):** 재개 후 nonce는 기억했지만 counter receipt의 `receipt:` 접두어를 생략했습니다. 정확한 값 복원 조건에 실패했으며, 중복 부작용 방지 조건은 통과했습니다.
- 13건 모두 원인 분류는 `undetermined`로 보존했습니다. 모델·bridge·시험 설계 원인을 통제 실험 없이 확정하지 않았습니다. 결과를 본 뒤 프롬프트·판정식을 완화하거나 재실행해 점수를 높이지 않았습니다.

## 재현·증거 보존

원본 증거(약 53 MiB)는 아래 로컬 폴더에 그대로 보존했습니다. 이 저장소에는 개인 경로·인증값·전체 대화 대신 70건의 체크 상태와 원본 파일 hash를 공개합니다. hash 목록만으로 원본 증거를 재검증할 수는 없습니다.

```bash
npm run test:compatibility -- --verify .runtime/workflows-live-20260921-003558/report.json
```

`evidenceIntegrity: true`, `fullMatrixPassed: false`입니다. 종료 코드 1은 이번 행렬에 실패가 있다는 뜻이지 무결성 오류가 아닙니다.

- Catalog SHA-256: `9013014390f817fd7af5bc12d1b8b89dcf36ca3a42ecffa3441bd35173836339`
- Implementation SHA-256: `afb98c1f714968fcbc194a24bc6cd99cd78193a26268adf1649c3dbb6d0515c8`
- Raw report SHA-256: `f12e773d90bba8ef230278d28414eb402eab2e296db07fe783e894684e2fa224`

기존 검증 시나리오·실행기·보고서 및 이전 실행 결과는 제거했습니다. 이번 원본 결과는 삭제하지 않았으며 `.git`의 과거 커밋 이력은 재작성하지 않았습니다.
