# 현재 구현 상태 — 2026-09-25

[English](STATUS.md) · [검증 안내](VERIFICATION_KO.md) · [최종 결과](validation/README_KO.md)

**codex-ghcp-essential-v1: 36/36 — 통과.** 실제 실행: **2026-09-25 08:19–08:25 KST**.

| 모델 | 통과 | 실패 케이스 |
| --- | --- | --- |
| claude-opus-5.5 | 6/6 | — |
| claude-sonnet-5 | 6/6 | — |
| claude-haiku-4.5 | 6/6 | — |
| gpt-6-astra | 6/6 | — |
| gpt-6-sol | 6/6 | — |
| gpt-6-luna | 6/6 | — |

**한 번의 실모델 실행에서 모델·시나리오 조합 36건과 공통 bridge 검사를 모두 통과**했으며 `fullMatrixPassed=true`, `evidenceIntegrity=true`입니다. 모든 모델의 V02에서 패치 전 테스트 실패와 종료 코드 1을 기록한 뒤, native `apply_patch`와 동일 테스트 3개 통과·종료 코드 0을 확인했습니다. 진단 작업 공간 스냅샷은 원본과 같았고 최종 스냅샷에서는 `discount.mjs`만 달랐으며, 두 체크포인트에서 테스트·기존 사용자 파일이 보존됐습니다.

## 검증한 절차와 한계

- **V02는 단계별 절차입니다:** 모든 모델에 동일한 두 턴을 적용합니다. 읽기 전용 진단에서 네이티브 실패 근거와 작업 공간 불변을 확인해야만 수정 요청을 보냅니다. 근거 누락·조기 수정·종료 코드 은폐는 보정 지시나 시나리오 재시도 없이 실패합니다.
- **유지한 조건:** 시나리오 6개·정확한 모델·제한 시간·36/36 기준·실제 테스트 실패 후 통과·명령과 결과의 원문 전송. 단계별 코딩 절차를 입증한 것이며 복합 요청 한 번의 지시 준수를 보장하지는 않습니다.

최종 마무리에서는 문서를 바로잡고 이 실행의 근거·미디어 참조를 재확인했습니다. 실행 코드·테스트·검증기는 기록된 소스 지문과 그대로 일치하며, 이번 문서 점검을 위한 실모델 재실행이나 모델 호출은 하지 않았습니다. 36/36은 위 시각의 실제 실행 결과입니다.

동일한 소스 지문에서 로컬 검사 **376/376**, 실제 Codex를 사용하는 오프라인 runtime 검사 **21/21**을 순차 통과한 뒤 실모델 검증을 진행했습니다. 실모델 전송·세션 오류는 없었고 전송·시작 시 목록 복구도 각각 0회였습니다. 복구 안전성은 제어된 회귀 검사로 확인했으며 이번 실행에서 실서비스 장애를 주입하지는 않았습니다.

[결과와 미디어](validation/README_KO.md)에 **101.76초 편집 녹화와 최신 스크린샷 8장**을 남겼으며 Sonnet V02의 두 단계도 포함했습니다. 전체 녹화·진단 체크포인트·원본 근거·동결 소스는 `.runtime/verification-final/`에 보관하고 무결성을 재계산했습니다.

핵심 CLI/TUI·도구 왕복·모델 전환·중단 복구·압축/재개만 검증합니다. 프로토콜·인증·대기열·시간 제한·핸드오프·재전송 방지 회귀 검사는 유지합니다. 멀티모달·스키마 강제 JSON·호스팅 도구·WebSocket·원격 압축은 미지원이며 장기 무인 운용은 미입증입니다.

현재 단일 기준·최신 실행·해당 미디어만 보존하며 전체 구현 완성도 백분율은 매기지 않습니다. 원본 실행 근거는 로컬에 있으며 Git에서 제외되므로 새 clone에는 포함되지 않습니다.
