# 현재 구현 상태 — 2026-09-25

[English](STATUS.md) · [검증 안내](VERIFICATION_KO.md) · [최종 결과](validation/README_KO.md)

**codex-ghcp-essential-v1: 34/36 — 미통과.** 실제 실행: **2026-09-25 10:58–11:06 KST**.

수정한 소스로 전체 36건을 한 번씩 실행했습니다. 통과 기준은 36/36으로 유지하며, 실패를 재시도하거나 이전 성공 결과로 대체하거나 점수에서 제외하지 않았습니다. `evidenceIntegrity=true`는 기록의 무결성을, `fullMatrixPassed=false`는 미통과 결과를 나타냅니다.

| 모델 | 통과 | 실패 케이스 |
| --- | --- | --- |
| claude-opus-5.5 | 5/6 | V04 |
| claude-sonnet-5 | 6/6 | — |
| claude-haiku-4.5 | 6/6 | — |
| gpt-6-astra | 6/6 | — |
| gpt-6-sol | 6/6 | — |
| gpt-6-luna | 5/6 | V02 |

## 실패 내역

| 케이스 | 관측한 실패 |
| --- | --- |
| Opus V04 | 최초 SDK `listModels` RPC가 10,570ms 후 실패해 모델 호출·피커 조작 전에 종료됐습니다. 이 전환 시나리오는 Astra로 시작하므로 Opus 전환에는 도달하지 못했습니다. 보존한 진단으로 RPC 오류는 확인되지만 그 아래의 네트워크·인증 원인은 확정할 수 없습니다. |
| Luna V02 | 네이티브 패치와 회귀 테스트 3개는 통과했지만, 최종 답변이 요청한 `sample.txt` 값 대신 수정 전 `discount.mjs` 소스였습니다. 필수 출력 검사가 실패했으므로 테스트 성공만으로 케이스를 통과시키지 않습니다. |

같은 소스 지문으로 로컬 단위·통합·문서 검사 **392/392**, 실제 Codex를 사용하는 오프라인 runtime 검사 **22/22**를 순차 통과한 뒤 이번 실모델 행렬을 실행했습니다. 검증한 소스에는 상주 bridge 상태 확인·중지와 SDK 가용성의 분리, 정리 실패 원인 표시 수정이 포함됩니다.

별도의 시작 시 모델 목록 시간 초과 2건은 원래 예산 안에서 복구됐습니다. Opus V04의 RPC 오류는 시간 초과 전용 복구 대상이 아니었습니다. 모델 전송 오류 복구는 없었습니다. 작업 공간 보존·MCP 격리·소유 프로세스 정리는 36건 모두 통과했지만 두 작업 흐름의 실패를 상쇄하지는 않습니다.

[최종 결과와 미디어](validation/README_KO.md)에 두 실패 모두와 스크린샷 10장·편집 영상을 포함했습니다. `.runtime/verification-final/`에는 이번 최종 실행만 남기고 이전 원본 결과·공개 미디어를 교체했습니다. 현재 소스와 동결 소스의 검증기 모두 근거 무결성을 확인하고 **34/36 미통과**를 재현합니다. 원본은 로컬에 있으며 Git에서 제외됩니다.

핵심 연동 결과이며 제품 전체 완성도나 장기 내구성 인증이 아닙니다. 미지원 기능의 범위는 바뀌지 않았습니다. 커밋별 [원격 CI 상태](https://github.com/junwoojeong100/openai-codex-ghcp-sdk/actions)는 이 실모델 점수와 별개입니다.
